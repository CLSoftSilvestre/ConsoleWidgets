namespace Widgets
{
    public enum BorderType
    {
        None = 0,
        Single = 1,
        Double = 2,
    }

    public enum BackgroundType
    {
        Solid = 0,
        Hard = 1,
        Medium = 2,
        Light = 3,
        Space = 4,
    }
}